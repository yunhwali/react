//es6 transpiling :
//모든 브라우저에서 es6가 작동하지는 않기 때문에, 
//브라우저에서 es6코드를 실행하기 전에 es5로 컴파일 해줌 
//가장 많이 쓰는 트랜스파일링 도구로는 바벨babel이 있다
//트랜스 파일링은 코드를 바이너리로 변환하는 것이 아니라는 점에서 컴파일과는 다르다.
//트랜스 파일링은 자바스크립트 코드를 더 많은 브라우저가 이해할 수 있다는 다른 버전의 자바스크립트 구문으로 변환하는 것이다.

//2-4
//바벨 트랜스파일링 이전의 es6코드
//const(상수 정의)
const add = (x=5, y=10) => console.log(x+y);

